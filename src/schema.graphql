# import * from './generated/prisma.graphql'

type Mutation {
  createProject(
    title: String
    shortDescription: String
    description: String
    amount: Int
    image: String
    largeImage: String
  ): Project!
}

type Query {
  projects: [Project]!
}
