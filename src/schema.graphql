# import * from './generated/prisma.graphql'

type Mutation {
  createProject(
    title: String
    description: String
    longDescription: String
    price: Int
    image: String
    largeImage: String
  ): Project!
}

type Query {
  projects: [Project]!
}
